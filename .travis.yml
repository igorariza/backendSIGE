language: python
python:
  - "3.6"
cache: pip
branches:
  only:
    - master
env:
  - DJANGO=3.0
before_install:
  - cd app/
install:
  - "pip install -r requirements.txt"
script:
  - python app/manage.py test
